/* 1. the output is one
2. the output is 1
3. the output is "login"
4. Welcome, that is a user: John

const userNames = ["Mike", "John", "Larry"];
const userInput = "John";
let htmlOutput = "";
if (userNames.indexOf(userInput) > -1) {
 htmlOutput = "Welcome, that is a user";
} else {
 htmlOutput = "Denied, not a user ";
}
console.log(htmlOutput + ": " + userInput);

5. output is "Wake up, it's morning."

6. let a = 5;
let b = 10;
let c = 20;
let d = 30;
console.log(a > b || b > a); true
console.log(a > b && b > a); false
console.log(d > b || b > a); true
console.log(d > b && b > a); true

7. unary operator: 1. 100 was LESS or Equal to 100;
100 is Even*/
